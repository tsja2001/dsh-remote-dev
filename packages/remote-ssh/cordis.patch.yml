# The dsh-remote-dev bundle layer: inserts the remote plugin row.
# Installed with:  dsh plugin --profile <name> add dsh-remote-dev
# The browser UI is discovered through the package's dsh.client declaration.
- insert:
    - id: remote-ssh
      name: 'dsh-remote-dev'
